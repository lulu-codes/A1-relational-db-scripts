-- 0_create_database.sql

DROP DATABASE IF EXISTS lash_appointment_system;

-- 2. Create new database (if already doesn't exist - below option without owner access for testing run)
CREATE DATABASE lash_appointment_system;
